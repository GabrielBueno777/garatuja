    // const nome: string = "João";
    // const idade: number = 30;
    // const isEstudante: boolean = true;

    // console.log(`Nome: ${nome}`);
    // console.log(`Idade: ${idade}`);
    // console.log(`É estudante? ${isEstudante ? "Sim" : "Não"}`);

    // function saudacao(nome: string): string {
    //     return `Olá, ${nome}! Bem-vindo ao TypeScript.`;
    // }

    // console.log(saudacao(nome));

    // interface Pessoa {
    //     nome: string;
    //     idade: number;
    //     isEstudante: boolean;
    // }

    // const pessoa: Pessoa = {
    //     nome: "Maria",
    //     idade: 25,
    //     isEstudante: false
    // };

    // console.log(`Pessoa: ${pessoa.nome}, Idade: ${pessoa.idade}, Estudante: ${pessoa.isEstudante ? "Sim" : "Não"}`);

    // type Estudante = {
    //     nome: string;
    //     idade: number;
    //     curso: string;
    // };

    // const estudante: Estudante = {
    //     nome: "Carlos",
    //     idade: 22,
    //     curso: "Engenharia de Software"
    // };

    // console.log(`Estudante: ${estudante.nome}, Idade: ${estudante.idade}, Curso: ${estudante.curso}`);  

    // enum Status {
    //     Ativo,
    //     Inativo,
    //     Pendente
    // }

    // const statusAtual: Status = Status.Ativo;
    // console.log(`Status atual: ${Status[statusAtual]}`);   
    
    //  function somar(a: number, b: number): number {
    //     return a + b;
    // }

    // console.log(`Soma: ${somar(5, 10)}`);

    // function multiplicar(a: number, b: number): number {
    //     return a * b;
    // }        

    // console.log(`Multiplicação: ${multiplicar(5, 10)}`);

    // function dividir(a: number, b: number): number {
    //     if (b === 0) {
    //         throw new Error("Divisão por zero não é permitida.");
    //     }
    //     return a / b;
    // }

    // try {
    //     console.log(`Divisão: ${dividir(10, 2)}`);
    //     console.log(`Divisão por zero: ${dividir(10, 0)}`);
    // } catch (error: unknown) {
    //     if (error instanceof Error) {
    //         console.error(error.message);
    //     }
    // }


